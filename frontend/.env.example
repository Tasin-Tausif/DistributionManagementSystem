# Local development uses the Vite proxy and keeps API requests on /api.
VITE_API_URL=/api

# Vite dev server target for proxying /api requests in local development.
VITE_PROXY_TARGET=http://127.0.0.1:8000